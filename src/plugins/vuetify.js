import { createVuetify } from 'vuetify';
import 'vuetify/styles'; // Ensure Vuetify styles are imported

// Create Vuetify instance with all components enabled
const vuetify = createVuetify();

export default vuetify;
